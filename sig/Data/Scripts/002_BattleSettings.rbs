# ==============================================================================
#
# ==============================================================================
module Settings
  # Whether turn order is recalculated after a Pok̩mon Mega Evolves.
  RECALCULATE_TURN_ORDER_AFTER_MEGA_EVOLUTION: bool

  # Whether turn order is recalculated after a Pok̩mon's Speed stat changes.
  RECALCULATE_TURN_ORDER_AFTER_SPEED_CHANGES: bool

  # Whether any Pok̩mon (originally owned by the player or foreign) can disobey
  # the player's commands if the Pok̩mon is too high a level compared to the
  # number of Gym Badges the player has.
  ANY_HIGH_LEVEL_POKEMON_CAN_DISOBEY: bool

  # Whether foreign Pok̩mon can disobey the player's commands if the Pok̩mon is
  # too high a level compared to the number of Gym Badges the player has.
  FOREIGN_HIGH_LEVEL_POKEMON_CAN_DISOBEY: bool

  # The Game Switch which, while ON, prevents all Pok̩mon in battle from Mega
  # Evolving even if they otherwise could.
  NO_MEGA_EVOLUTION: Integer

  # Whether a move's physical/special category depends on the move itself as in
  # newer Gens (true), or on its type as in older Gens (false).
  MOVE_CATEGORY_PER_MOVE: bool

  # Whether critical hits do 1.5x damage and have 4 stages (true), or they do 2x
  # damage and have 5 stages as in Gen 5 (false). Also determines whether
  # critical hit rate can be copied by Transform/Psych Up.
  NEW_CRITICAL_HIT_RATE_MECHANICS: bool

  # Whether several effects apply relating to a Pok̩mon's type:
  #   * Electric-type immunity to paralysis
  #   * Ghost-type immunity to being trapped
  #   * Grass-type immunity to powder moves and Effect Spore
  #   * Poison-type Pok̩mon can't miss when using Toxic
  MORE_TYPE_EFFECTS: bool

  # The minimum number of Gym Badges required to boost each stat of a player's
  # Pok̩mon by 1.1x, in battle only.
  NUM_BADGES_BOOST_ATTACK: Integer

  NUM_BADGES_BOOST_DEFENSE: Integer

  NUM_BADGES_BOOST_SPATK: Integer

  NUM_BADGES_BOOST_SPDEF: Integer

  NUM_BADGES_BOOST_SPEED: Integer

  # Whether weather caused by an ability lasts 5 rounds (true) or forever (false).
  FIXED_DURATION_WEATHER_FROM_ABILITY: bool

  # Whether X items (X Attack, etc.) raise their stat by 2 stages (true) or 1
  # (false).
  X_STAT_ITEMS_RAISE_BY_TWO_STAGES: bool

  # Whether some Pok̩ Balls have catch rate multipliers from Gen 7 (true) or
  # from earlier generations (false).
  NEW_POKE_BALL_CATCH_RATES: bool

  # Whether Soul Dew powers up Psychic and Dragon-type moves by 20% (true) or
  # raises the holder's Special Attack and Special Defense by 50% (false).
  SOUL_DEW_POWERS_UP_TYPES: bool

  # Whether Pok̩mon with high happiness will gain more Exp from battles, have a
  # chance of avoiding/curing negative effects by themselves, resisting
  # fainting, etc.
  AFFECTION_EFFECTS: bool

  # Whether a Pok̩mon's happiness is limited to 179, and can only be increased
  # further with friendship-raising berries. Related to AFFECTION_EFFECTS by
  # default because affection effects only start applying above a happiness of
  # 179. Also lowers the happiness evolution threshold to 160.
  APPLY_HAPPINESS_SOFT_CAP: bool

  # Whether the critical capture mechanic applies. Note that its calculation is
  # based on a total of 600+ species (i.e. that many species need to be caught
  # to provide the greatest critical capture chance of 2.5x), and there may be
  # fewer species in your game.
  ENABLE_CRITICAL_CAPTURES: bool

  # Whether the player is asked what to do with a newly caught Pok̩mon if their
  # party is full. If true, the player can toggle whether they are asked this in
  # the Options screen.
  NEW_CAPTURE_CAN_REPLACE_PARTY_MEMBER: bool

  # Whether the Exp gained from beating a Pok̩mon should be scaled depending on
  # the gainer's level.
  SCALED_EXP_FORMULA: bool

  # Whether the Exp gained from beating a Pok̩mon should be divided equally
  # between each participant (true), or whether each participant should gain
  # that much Exp (false). This also applies to Exp gained via the Exp Share
  # (held item version) being distributed to all Exp Share holders.
  SPLIT_EXP_BETWEEN_GAINERS: bool

  # Whether the Exp gained from beating a Pok̩mon is multiplied by 1.5 if that
  # Pok̩mon is owned by another trainer.
  MORE_EXP_FROM_TRAINER_POKEMON: bool

  # Whether a Pok̩mon holding a Power item gains 8 (true) or 4 (false) EVs in
  # the relevant stat.
  MORE_EVS_FROM_POWER_ITEMS: bool

  # Whether Pok̩mon gain Exp for capturing a Pok̩mon.
  GAIN_EXP_FOR_CAPTURE: bool

  # The Game Switch which, whie ON, prevents the player from losing money if
  # they lose a battle (they can still gain money from trainers for winning).
  NO_MONEY_LOSS: Integer

  # Whether party Pok̩mon check whether they can evolve after all battles
  # regardless of the outcome (true), or only after battles the player won (false).
  CHECK_EVOLUTION_AFTER_ALL_BATTLES: bool

  # Whether fainted Pok̩mon can try to evolve after a battle.
  CHECK_EVOLUTION_FOR_FAINTED_POKEMON: bool

  # Whether wild Pok̩mon with the "Legendary", "Mythical" or "UltraBeast" flag
  # (as defined in pokemon.txt) have a smarter AI. Their skill level is set to
  # 32, which is a medium skill level.
  SMARTER_WILD_LEGENDARY_POKEMON: bool
end


